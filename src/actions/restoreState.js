/**
 * Given an object, sets the store.
 *
 * @param {Object} - state
 * @param {Object} - savedState
 * @return {Object}
 */
const restoreState = (state, savedState) => {
  return savedState;
};

export default restoreState;
