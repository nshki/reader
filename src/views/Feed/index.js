import React from 'react';
import { FeedItems } from '../../components';

class Feed extends React.Component {
  render() {
    return (
      <React.Fragment>
        <FeedItems />
      </React.Fragment>
    );
  }
}

export default Feed;
